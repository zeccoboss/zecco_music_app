import appConfig from "../config/AppConfig";

// Render Register form
const RenderVerifyPasswordPage = async () => {
	appConfig.pageTitle = "Verify Password";
};

export default RenderVerifyPasswordPage;
